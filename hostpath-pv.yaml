apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-hp-pv
spec:
  capacity: 
    storage: 4Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /tmp/mongodb
  storageClassName: localstorage
